# Vocitap 版本更新日志 (CHANGELOG)

本文档用于记录 Vocitap 语音输入法的每一个版本迭代细节。

---

## [5.7.4] - 2026-08-10
### Bug 修复
- **OTA 重连硬件时序防丢包修复**：针对部分 CH340/CP2102 串口芯片在打开端口（open）时拉低 DTR/RTS 触发硬件冷启动物理复位的隐患，在 OTA 重连中增加了 1.5 秒的防复位硬件稳定延时，彻底根成了由于硬件冷启动初始化未完成引起的第二次握手丢包及 OTA 升级失败问题。

---

## [5.7.3] - 2026-08-10
### 架构转型
- **macOS 分发解耦**：根据跨平台开发调整，正式从 Windows 开发端 Git 中剔除并全量忽略 `mac_app/` 资产目录，由 macOS 端的开发环境自主编译并独立推送该平台 App。

### 功能增强
- **有线串口 OTA 更新稳定性升级**：跟进配套硬件固件的最新大升级，重构了 `upload_firmware` 流控算法。
  - **重启物理重连**：当握手阻碍硬件需要重启至 clean OTA 状态时，主机会自动 disconnect 连接并休眠 3.0 秒，随后自动 reconnect 完成二次握手；
  - **微数据包防溢出**：分包字节由 1024 字节压缩为 256 字节，物理层面防范 UART FIFO 硬件过载溢出；
  - **16KB 周期同步**：取消以前的每字节卡死等待，改用每发送 16KB 才强制执行一次的严格对齐确认，并且在发送尾包前做一次前置整数对齐，彻底阻断了数据风暴与不对齐引起的假死、死锁现象。

---

## [5.7.2] - 2026-08-03
### 功能增强
- **配套硬件配置协议适配**：根据 ESP32 键盘固件的最新调整，将“自动休眠模式”配置变更为“启用esp32麦克风”开关，以更好地服务于无麦克风台式机用户的语音录入需求。
- **自动重启指引与气泡提示**：在界面勾选框上新设了悬浮 ToolTip 指示，提示用户选择配置并同步到硬件 NVS 后，设备端将自动触发软重启。

---

## [5.7.1] - 2026-07-13
### 功能增强
- **连接面板排版优化**：重构有线串口连接模块为两行排布，将 HFP 连接状态、Audio 链路状态与硬件固件版本号独立折行并并排展示，大幅压缩控制面板横向宽度，消除窄窗口遮挡问题。
- **固件卡片精简**：移除了固件在线升级模块中重复的版本指示标签，实现所有高级状态信息在同一处合并展示。

---

## [5.7.0] - 2026-07-13
### 架构转型
- **有线串口替换蓝牙**：将键盘配置和连接逻辑由原来的 BLE 蓝牙连接全面升级为 USB 物理有线串口（SPP）连接，大幅提升通信稳定性和传输速率。
- **依赖库精简**：从 `requirements.txt` 中彻底移除 `bleak` 蓝牙库，引入 `pyserial>=3.5` 串口通信库。
- **分发包架构隔离**：新建 `windows_app/` 和 `mac_app/` 独立分发目录，各自包含影子启动器、编译脚本和可执行配置文件。

### 功能增强
- **串口选择交互**：在硬件配置页面新增可用串口下拉菜单 `QComboBox` 与“连接/断开”动作按钮，支持用户手动指定连接端口。
- **连接面板排版优化**：重构串口连接模块为两行排布，将 HFP 连接状态、Audio 音频状态与硬件固件版本号独立折行并并排展示，大幅压缩横向宽度，彻底消除窄窗口遮挡问题。
- **自动重连机制**：保留最后一次成功连接的串口配置（`last_device_address`），在程序启动时如果检测到设备已插入，会自动进行物理重连。
- **串口 OTA 固件升级**：替换了原先的 BLE 写入逻辑，改为调用经过流控校验的串口 JSON Chunk OTA 协议，极大减少了由于无线信号波动导致升级中断的概率。
- **打包启动与环境自动补全**：在 Windows 本地编译生成了 5.7.0 版单文件 `Vocitap.exe` 分发包，并在启动器的依赖清单中引入了 `pyserial` 的静默补齐机制。
- **代码库净化**：清理了根目录下过期的 spec、exe、rar 及备份文件夹，全部移入 `_archive` 目录，规范了项目文件布局。

### Bug 修复
- **影子部署版本同步**：修复了原启动器逻辑中在 `env_ready.tag` 存在时会自动跳过版本比对、导致更新 EXE 后 AppData 源码不覆盖的严重 Bug。提取比对机制为外层强制执行，保证每次运行均能完成源码影子同步。

---

## [5.6.4] - 2026-06-05
### 功能增强
- **固件在线升级 (OTA)**：全面集成 BLE 固件更新功能，支持通过蓝牙分片传输 `.bin` 固件包。
- **版本实时反馈**：在硬件设置页面新增硬件固件版本显示，并在连接成功后自动刷新。
- **传输稳定性**：实现了基于 500 字节分片与 0.02s 间隔的可靠传输协议，内置升级进度条与实时状态提示。

### UI & 体验
- **布局扩容**：将窗口高度微调至 800 像素，完美承载新增的 OTA 升级面板。
- **交互保护**：升级期间自动更新状态文字，引导用户等待设备重启。

---

## [5.6.3] - 2026-05-30
### 架构转型
- **被动配置器模式**：由“连接管理器”转型为“硬件配置面板”，不再干预系统的蓝牙连接生命周期。
- **即插即用识别**：移除所有地址记忆逻辑，改用基于 **Service UUID (0x1820)** 的后台静默发现。

### 功能增强
- **4 按键全支持**：底层协议与 UI 界面全面升级，支持 4 路独立按键映射（新增 UUID: 0x2A08）。
- **手动刷新机制**：在硬件设置页新增“刷新状态”按钮，由用户自主控制 BLE 探测时机，降低后台开销。

### UI & 体验
- **尺寸精简**：窗口高度从 850 压缩至 750，适配移除连接管理按钮后的布局。
- **刷新按钮优化**：增加了交互逻辑保护，已连接状态下自动禁用刷新按钮。
- **绿色扫描策略**：移除了后台自动循环扫描，并将单次扫描耗时压缩至 3 秒，优先利用 Windows 设备缓存，彻底解决了扫描干扰 HID 链路导致键盘断连的问题。
- **日志标记**：启动时增加 GUI 版本加载标记，便于源码调试。
- **崩溃修复**：彻底清理了 GUI 层对已删除按钮（scan_btn）的残留引用。

### 管理优化
- **文档维护自动化**：在 `GEMINI.md` 中确立了强制同步更新日志的准则，确保每一次技术迭代都能被实时记录。

---

## [5.6.2] - 2026-05-28
### 硬件进阶
- **BLE 功率控制**：支持 -12dBm 至 +9dBm 的 8 档发射功率调节。
- **自动休眠模式**：支持远程开启/关闭硬件的节能深度休眠逻辑。
- **8 修饰键回归**：补全 L/R 区分的 Ctrl, Shift, Alt, Win 共 8 个修饰键勾选功能。

### 稳定性加固
- **BLE 状态大一统**：建立“逻辑至上”信任模型，过滤驱动层瞬时 False 导致的 UI 闪跳。
- **影子部署优化**：在 `launcher.py` 中实现版本校验，支持 EXE 更新后自动覆盖 AppData 源码。
- **退出保护**：将 `os._exit(0)` 与 `icon.stop()` 结合，解决多线程环境下的退出卡死与托盘残留。

---

## [5.6.1] - 2026-05-15
### 核心稳定版
- **单实例锁定**：引入 Windows 命名互斥量 (Named Mutex)，彻底解决多实例冲突导致的托盘死锁。
- **基础架构**：确立了“单 EXE 启动器 + AppData 影子代码”的双模运行架构。

---

## [历史版本]
- **5.3**: 实现纯净架构，源码直接从 Temp 运行，减少 AppData 污染。
- **5.0**: 首次集成 BLE 硬件支持，加入设备设置选项卡。
- **4.9**: 增加右侧修饰键支持与 Bilingual README。
